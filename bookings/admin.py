"""Admin configuration for the bookings app.

This module is intended for registering Booking and Ticket models with the
Django admin interface, allowing administrators to manage bookings and tickets
directly via the admin panel.
"""
from django.contrib import admin

# Register your models here.
